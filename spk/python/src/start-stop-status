#!/bin/sh

case $1 in
        start)
                exit 0
        ;;
        stop)
                exit 0
        ;;
        status)
                exit 1
        ;;
        log)
                echo "${SYNOPKG_PKGDEST}/output.log"
                exit 0
        ;;
esac