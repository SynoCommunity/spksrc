[repository]
ROOT = ${SHARE_PATH}/gitea-repositories
[server]
SSH_DOMAIN = ${IP:=localhost}
DOMAIN = ${IP:=localhost}
ROOT_URL = http://${IP:=localhost}:${SERVICE_PORT}/
[lfs]
PATH = ${SHARE_PATH}/lfs