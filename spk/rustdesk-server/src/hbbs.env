# RustDesk ID/Rendezvous Server Environment Configuration
# This is a community-created template based on RustDesk source code analysis.
# It is NOT an official RustDesk configuration file.
#
# Location: /var/packages/rustdesk-server/var/hbbs.env
# Restart the service after making changes.
# Reference: https://rustdesk.com/docs/en/self-host/
#
# Format: Standard shell environment file (VAR=value)
# Lines starting with # are comments. Uncomment to enable options.

# ============================================================================
# RELAY SETTINGS
# ============================================================================

# Force all connections through relay server (no direct P2P)
# Useful for restrictive network environments or enhanced privacy
# Values: Y or N (default: N)
#ALWAYS_USE_RELAY=N

# Default relay server(s) for clients
# Comma-separated list of host:port (default port 21117 if omitted)
# Leave empty to use the local relay server (hbbr)
#RELAY-SERVERS=

# ============================================================================
# NETWORK SETTINGS  
# ============================================================================

# LAN detection mask (CIDR notation)
# Connections from IPs within this range are treated as local/LAN
# Example: 192.168.0.0/16 or 10.0.0.0/8
#MASK=

# Local IP address (auto-detected if not set)
# Used with mask for LAN detection
#LOCAL-IP=

# ============================================================================
# CLIENT SOFTWARE UPDATE
# ============================================================================

# URL for RustDesk client software download
# Clients will be notified of updates from this URL
#SOFTWARE-URL=

# ============================================================================
# SECURITY
# ============================================================================

# Restrict connections to clients with matching key
# The key file (id_ed25519.pub) is auto-generated on first start
# Clients must be configured with this key to connect
# Note: Use -k command line option or KEY environment variable
#KEY=

# ============================================================================
# ADVANCED
# ============================================================================

# Configuration serial number (for cluster synchronization)
# Increment when config changes to notify other servers
#SERIAL=0

# UDP receive buffer size in bytes (requires system tuning)
# To increase: sudo sysctl -w net.core.rmem_max=52428800
# Then set RMEM to desired value (0 = system default)
#RMEM=0
