# ===========================================
# Pelican Panel - Configuration Environment
# ===========================================
# Ce fichier est généré automatiquement lors de l'installation.
# Les valeurs sont remplacées par le wizard d'installation.

# --- Package Wiring (Ne pas modifier) ---
COMPOSE_PROJECT_NAME=pelican_panel
DATA_ROOT=/var/packages/pelican_panel/var/data
INSTALL_DIR=/var/packages/pelican_panel/target

# --- Docker Images ---
PANEL_IMAGE=ghcr.io/pelican-dev/panel:latest

# --- Application Settings ---
APP_ENV=production
APP_DEBUG=false
APP_KEY=
APP_URL=http://localhost:8080
APP_TIMEZONE=Europe/Paris

# --- Caddy SSL (email for Let's Encrypt, not used for admin) ---
ADMIN_EMAIL=admin@localhost

# --- Pre-configured defaults for wizard ---
# These are the default values that Pelican wizard will use.
# The user just needs to click "Next" on each step.
# Database: SQLite (default)
# Cache: File (default)
# Queue: Sync (default)
# Session: File (default)

# --- Ports ---
PANEL_PORT=8080
PANEL_INTERNAL_PORT=8090
PANEL_HTTPS_PORT=8444

# --- Wings Ports ---
WINGS_PORT=8445
WINGS_SFTP_PORT=2022

# --- Session & Cookies ---
# Mettre à 'true' si vous utilisez un reverse proxy HTTPS
SESSION_SECURE_COOKIE=false

# --- Proxy Settings ---
# '*' pour accepter tous les proxies (reverse proxy DSM)
TRUSTED_PROXIES=*

# --- Mail Settings (Optionnel) ---
# Configurable via le panneau d'administration après installation
MAIL_DRIVER=log
MAIL_HOST=
MAIL_PORT=587
MAIL_FROM=
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls

# ===========================================
# Wings Settings (Après configuration Panel)
# ===========================================
# Ces valeurs sont obtenues après création du Node dans le Panel
WINGS_FQDN=
WINGS_TOKEN_ID=
WINGS_TOKEN=
WINGS_PANEL_URL=http://localhost:8080
WINGS_CONFIG_PATH=/var/packages/pelican_panel/var/data/wings/config.yml
