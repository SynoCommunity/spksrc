--- meson.build-orig	2026-08-01 00:00:00.000000000 +0000
+++ meson.build	2026-08-01 00:00:00.000000000 +0000
@@ -21,8 +21,8 @@ cy = meson.get_compiler('cython')
 
 # Check compiler is recent enough (see the SciPy Toolchain Roadmap for details)
 if cc.get_id() == 'gcc'
-  if not cc.version().version_compare('>=10.3')
-    error('NumPy requires GCC >= 10.3')
+  if not cc.version().version_compare('>=8.5')
+    error('NumPy requires GCC >= 8.5')
   endif
 elif cc.get_id() == 'msvc'
