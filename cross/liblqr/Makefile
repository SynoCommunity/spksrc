PKG_NAME = liblqr
PKG_VERS = 0.4.3
PKG_EXT = tar.gz
PKG_DIST_NAME = v$(PKG_VERS).$(PKG_EXT)
PKG_DIST_SITE = https://github.com/carlobaldassi/liblqr/archive
PKG_DIST_FILE = $(PKG_NAME)-$(PKG_VERS).$(PKG_EXT)
PKG_DIR = $(PKG_NAME)-$(PKG_VERS)

DEPENDS = cross/glib

GNU_CONFIGURE = 1

HOMEPAGE = https://github.com/carlobaldassi/liblqr
COMMENT  = Liquid Rescale library.
LICENSE  = LGPLv3

include ../../mk/spksrc.cross-cc.mk
