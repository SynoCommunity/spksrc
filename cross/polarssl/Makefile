PKG_NAME = polarssl
PKG_VERS = 1.3.2
PKG_EXT = tgz
PKG_DIST_NAME = $(PKG_NAME)-$(PKG_VERS)-gpl.$(PKG_EXT)
PKG_DIST_SITE = https://tls.mbed.org/download
PKG_DIR = $(PKG_NAME)-$(PKG_VERS)

DEPENDS =

HOMEPAGE = https://tls.mbed.org
COMMENT  = This is a small release adds a few new features but mostly increases support for different platforms and interoperability with other libraries.
LICENSE = GPL

CONFIGURE_ARGS =

GNU_CONFIGURE = 1
CONFIGURE_TARGET = nop

include ../../mk/spksrc.cross-cc.mk
