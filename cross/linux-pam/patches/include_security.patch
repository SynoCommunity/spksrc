--- libpam/Makefile.in.orig	2015-03-18 00:45:11.326276530 +0100
+++ libpam/Makefile.in	2015-03-18 00:55:24.450764190 +0100
@@ -268,6 +268,7 @@
 host_vendor = @host_vendor@
 htmldir = @htmldir@
 includedir = @includedir@
+includesecuritydir = $(includedir)/security
 infodir = @infodir@
 install_sh = @install_sh@
 libc_cv_fpie = @libc_cv_fpie@
@@ -460,24 +461,24 @@
 	-rm -rf .libs _libs
 install-includeHEADERS: $(include_HEADERS)
 	@$(NORMAL_INSTALL)
-	test -z "$(includedir)" || $(MKDIR_P) "$(DESTDIR)$(includedir)"
-	@list='$(include_HEADERS)'; test -n "$(includedir)" || list=; \
+	test -z "$(includesecuritydir)" || $(MKDIR_P) "$(DESTDIR)$(includesecuritydir)"
+	@list='$(include_HEADERS)'; test -n "$(includesecuritydir)" || list=; \
 	for p in $$list; do \
 	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
 	  echo "$$d$$p"; \
 	done | $(am__base_list) | \
 	while read files; do \
-	  echo " $(INSTALL_HEADER) $$files '$(DESTDIR)$(includedir)'"; \
-	  $(INSTALL_HEADER) $$files "$(DESTDIR)$(includedir)" || exit $$?; \
+	  echo " $(INSTALL_HEADER) $$files '$(DESTDIR)$(includesecuritydir)'"; \
+	  $(INSTALL_HEADER) $$files "$(DESTDIR)$(includesecuritydir)" || exit $$?; \
 	done
 
 uninstall-includeHEADERS:
 	@$(NORMAL_UNINSTALL)
-	@list='$(include_HEADERS)'; test -n "$(includedir)" || list=; \
+	@list='$(include_HEADERS)'; test -n "$(includesecuritydir)" || list=; \
 	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
 	test -n "$$files" || exit 0; \
-	echo " ( cd '$(DESTDIR)$(includedir)' && rm -f" $$files ")"; \
-	cd "$(DESTDIR)$(includedir)" && rm -f $$files
+	echo " ( cd '$(DESTDIR)$(includesecuritydir)' && rm -f" $$files ")"; \
+	cd "$(DESTDIR)$(includesecuritydir)" && rm -f $$files
 
 ID: $(HEADERS) $(SOURCES) $(LISP) $(TAGS_FILES)
 	list='$(SOURCES) $(HEADERS) $(LISP) $(TAGS_FILES)'; \
