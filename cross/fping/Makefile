PKG_NAME = fping
PKG_VERS = 3.10
PKG_EXT = tar.gz
PKG_DIST_NAME = $(PKG_NAME)-$(PKG_VERS).$(PKG_EXT)
PKG_DIST_SITE = http://$(PKG_NAME).org/dist
PKG_DIR = $(PKG_NAME)-$(PKG_VERS)

DEPENDS = 

HOMEPAGE = http://fping.org/
COMMENT  = 
LICENSE  =

GNU_CONFIGURE = 1
CONFIGURE_ARGS = --enable-ipv4 --enable-ipv6

include ../../mk/spksrc.cross-cc.mk


	