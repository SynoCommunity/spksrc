PKG_NAME = popt
PKG_VERS = 1.10.2
PKG_EXT = tar.gz
PKG_DIR = $(PKG_NAME)-$(PKG_VERS)
PKG_DIST_NAME = $(PKG_DIR).$(PKG_EXT)
PKG_DIST_SITE = http://rpm5.org/files/popt

DEPENDS =

HOMEPAGE = http://rpm5.org/files/popt/
COMMENT  =
LICENSE  =

GNU_CONFIGURE = 1
CONFIGURE_ARGS = --disable-static --enable-shared

include ../../mk/spksrc.cross-cc.mk
