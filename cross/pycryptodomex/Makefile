PKG_NAME = pycryptodomex
PKG_VERS = 3.10.1
PKG_EXT = tar.gz
PKG_DIST_NAME = $(PKG_NAME)-$(PKG_VERS).$(PKG_EXT)
PKG_DIST_SITE = https://files.pythonhosted.org/packages/source/p/$(PKG_NAME)
PKG_DIR = $(PKG_NAME)-$(PKG_VERS)

DEPENDS =

HOMEPAGE = https://www.pycryptodome.org/
COMMENT  = PyCryptodome is a self-contained Python package of low-level cryptographic primitives. With pycryptodomex: PyCrypto and PyCryptodome can coexist.
LICENSE  = 2-Clause BSD

include ../../mk/spksrc.python-wheel.mk
