PKG_NAME = protobuf-c
PKG_VERS = 0.15
PKG_EXT = tar.gz
PKG_DIST_NAME = $(PKG_NAME)-$(PKG_VERS).$(PKG_EXT)
PKG_DIST_SITE = http://protobuf-c.googlecode.com/files
PKG_DIR = $(PKG_NAME)-$(PKG_VERS)

DEPENDS = 

HOMEPAGE   = http://code.google.com/p/protobuf-c/
COMMENT    = Code generator and runtime libraries to use Protocol Buffers from pure C
LICENSE    = 

GNU_CONFIGURE = 1
CONFIGURE_ARGS = --disable-protoc
 
include ../../mk/spksrc.cross-cc.mk

