PKG_NAME = lowdown
PKG_VERS = 2_0_4
PKG_EXT = tar.gz
PKG_DIST_NAME = $(PKG_NAME)-$(PKG_VERS).$(PKG_EXT)
PKG_DIST_SITE = https://github.com/kristapsdz/lowdown/archive/refs/tags/VERSION_$(PKG_VERS)
PKG_DIR = $(PKG_NAME)-VERSION_$(PKG_VERS)

HOMEPAGE = https://kristaps.bsd.lv/lowdown
COMMENT  = lowdown is a Markdown translator producing HTML5, roff documents in the ms and man formats, LaTeX, gemini (“gemtext”), OpenDocument, and ANSI/UTF8 terminal output.
LICENSE  = ISC

include ../../mk/spksrc.cross-cc.mk
