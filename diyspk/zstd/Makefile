SPK_NAME = zstd
SPK_VERS = 1.5.6
SPK_REV = 1
SPK_ICON = src/zstd.png

DEPENDS = cross/zstd

MAINTAINER = SynoCommunity
DESCRIPTION = Zstandard is a fast compression algorithm, providing high compression ratios.
DISPLAY_NAME = Zstandard
STARTABLE = no

HOMEPAGE = https://facebook.github.io/zstd/
LICENSE  = GPLv2/BSD

SPK_COMMANDS = bin/zstd bin/unzstd bin/zstdcat bin/zstdmt bin/zstdgrep bin/zstdless bin/zstd-frugal

include ../../mk/spksrc.spk.mk
